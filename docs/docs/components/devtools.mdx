import {AsideLive} from '../../src/components/AsideLive'
import {ComponentProps} from '../../src/components/ComponentProps/ComponentProps'

# Devtools

The devtools component is used to connect to the browser extension devtools and setup the remote rendering bridge.

It optionally accepts a list of capabilities to enable experimental features.

## DevtoolsProps

<ComponentProps name="Devtools" />
