import {AsideLive} from '../../src/components/AsideLive'
import {ComponentProps} from '../../src/components/ComponentProps/ComponentProps'

# Button

Buttons allow users to take actions, and make choices, with a single tap.

## Examples

Currently buttons are only transparent containers for icons to allow capturing interactions.

<AsideLive
  centered
  code={
`<Button title="Clear" size="icon" onClick={() => alert('clicked')}>
  <Icon source="cancel" />
</Button>`
    }
/>

## ButtonProps

<ComponentProps name="Button" />
