import {AsideLive} from '../../src/components/AsideLive'
import {ComponentProps} from '../../src/components/ComponentProps/ComponentProps'

# View

The most fundamental component for building a UI. View is a container that supports layout through `className`, giving users access to most of [tailwindcss](https://tailwindcss.com/)'s classes.

## Examples

<AsideLive
  centered
  code={
`<View className="text-console-number">
  I'm a view
</View>`
    }
/>

## ViewProps

<ComponentProps name="View" />
